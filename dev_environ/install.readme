To install dev environment
------------------------

sudo apt-get remove openocd //remove old versions of openocd
tar -xvf openocd_dev.tar.gz
cd openocd
sudo apt-get install make libtool pkg-config autoconf automake texinfo libusb-1.0-0-dev //install depen.

./bootstrap
./configure
make
sudo make install

